services:
  garmin-nostra:
    build: .
    container_name: garmin-nostra
    restart: unless-stopped
    volumes:
      - ~/data/garminnostra:/data
      - ./config.toml:/app/config.toml:ro
    environment:
      - CONFIG_FILE=/app/config.toml
      - TZ=Europe/Berlin
